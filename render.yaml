services:
  - type: web
    name: alfred-eyes-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python auto_reply_bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
